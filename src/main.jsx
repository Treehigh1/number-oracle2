import React from 'react';
import ReactDOM from 'react-dom/client';
import LotteryBot from './lottery-prediction-bot.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LotteryBot />
  </React.StrictMode>
);
